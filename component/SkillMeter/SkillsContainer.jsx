import { motion } from 'framer-motion';
import React from 'react';

const SkillsContainer = ({ children,variants, intial, show, }) => {
  return <motion.div>

  </motion.div>;
};

export default SkillsContainer;
